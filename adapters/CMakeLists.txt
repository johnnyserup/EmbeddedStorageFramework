# ---------------------------------------------------------------------------
# adapters/ — Platform adapter targets
#
# This is a placeholder CMakeLists.txt, ready for future adapter sub-targets.
# Add one add_subdirectory() call per platform adapter as they are implemented.
#
# Example:
#   add_subdirectory(stm32_hal)
# ---------------------------------------------------------------------------
